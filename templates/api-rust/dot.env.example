# Placeholder values only. Copy to .env and replace before running.
# Never commit a real .env file.

SECRET_KEY=change-me
ENV=development
BIND_ADDR=127.0.0.1:8000
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
